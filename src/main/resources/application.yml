server:
  servlet:
    context-path: /api
  port: '8081'
  
spring:
  datasource:
    password: q1w2e3r4
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dbuser
    url: jdbc:mysql://localhost:3306/project?serverTimezone=UTC
  jpa:
    properties:
      hibernate:
        "[format_sql]": 'true'
        "[show_sql]": 'true'
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create

hibernate:
  use-new-id-generator-mappings:false

mybatis:
  type-aliases-package: com.example.mappertut.model
  mapper-locations: classpath:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: 'true'
    
logging:
  level:
    jdbc:
      audit: 'off'
      connection: 'off'
      sqlonly: info
      resultset: 'off'
      resultsettable: info
      sqltiming: 'off'

jwt:
  secretKey: 7J206rKD7J2A6rCc7J247ZSE66Gc7KCd7Yq466W87JyE7ZWcand0c2VjcmV0a2V57J6F64uI64uk #base64로 인코딩된 암호 키, HS512를 사용할 것이기 때문에, 512비트(64바이트) 이상이 되어야 합니다. 영숫자 조합으로 아무렇게나 길게 써주세요!
  access:
    expiration: 3600000 # 1시간(60분) (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h))
    header: Authorization
  refresh:
    expiration: 1209600000 #  (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h) * 24L(h -> 하루) * 14(2주))
    header: Authorization-refresh